
export default {
  navigator: null,         // reference to the iOS navigator
  tokens: [],              // [{tok}, {tok}, ...]
  addresult: '',           // the last result from adding a token
  increaser: 0,            // dummy value to fiddle with to cause redraw
}
